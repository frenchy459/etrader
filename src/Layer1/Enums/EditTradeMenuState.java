package Layer1.Enums;

/** Represents the different types of states available for the Edit Trade menu */
public enum EditTradeMenuState {

    /** Represents the state where a new trade is being edited */
    NEW,

    /** Represents the state where a temporary trade is being edited */
    TEMPORARY,

    /** Represents the state where an existing trade is being edited */
    EXISTING
}
